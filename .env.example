# 复制为 .env 并填入真实值。.env 已被 .gitignore 忽略,切勿提交。
#
# 排盘(起盘)是本地确定性计算,不需要任何 key、不花钱。
# 只有「AI 解读 / 对话」用到下面的 provider。
#
# 默认 = pollinations:免费、免 key、开箱即用,clone 完不配置也能直接跑。
# 代价:用的是小模型,中文质量一般、可能限速。想要好效果就切到下面任意一家。
#
# ── 选 provider(默认 pollinations,无需任何 key) ────────────────────────
# 可选:
#   pollinations  免费·免 key·在线·开箱即用(默认;质量一般)
#   ollama        本地模型,免费免 key、不联网(需先装 Ollama 并拉模型;质量取决于模型)
#   glm           智谱 GLM-4-Flash,免费但需 key,国内直连、中文强(推荐升级项)
#   gemini        免费额度需 key  /  groq 免费需 key  /  anthropic 付费  /  custom 自定义
AI_PROVIDER=pollinations

# ── ollama:无需任何 key。装好 Ollama 后改用这几行 ──────────────────────
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434/v1   # 默认即此,通常不用改
# AI_MODEL_REPORT=qwen2.5:32b                 # 可换 qwen2.5:72b / qwen2.5:14b 等
# AI_MODEL_CHAT=qwen2.5:32b

# ── 云端 provider:按所选填对应一把 key 即可 ─────────────────────────────
# 智谱 GLM-4-Flash(完全免费,国内直连):https://open.bigmodel.cn/
GLM_API_KEY=

# Google Gemini(免费额度,通常需科学上网):https://aistudio.google.com/apikey
# GEMINI_API_KEY=

# Groq(免费,极快;中文偏弱):https://console.groq.com/keys
# GROQ_API_KEY=

# Anthropic Claude(付费,质量最高):https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# ── 可选:覆盖模型名 / 自定义 OpenAI 兼容端点 ────────────────────────────
# AI_MODEL_REPORT=
# AI_MODEL_CHAT=
# AI_PROVIDER=custom 时需要(任意 OpenAI 兼容服务,如 OpenRouter / DeepSeek / 本地 Ollama):
# AI_BASE_URL=
# AI_API_KEY=
